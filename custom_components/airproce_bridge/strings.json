{
  "title": "AirProce",
  "config": {
    "step": {
      "user": {
        "title": "Set up AirProce",
        "description": "AirProce connects directly to Home Assistant through the purifier's USR Socket B. MQTT is not required. Keep Socket A connected to the official AirProce cloud if you want to continue using the official app.",
        "sections": {
          "basic": {"name": "Purifier"},
          "advanced": {"name": "Advanced settings"}
        },
        "data": {
          "device_name": "Device name",
          "device_model": "Purifier model",
          "usr_host": "USR module IP address",
          "listen_port": "Socket B listening port",
          "usr_web_port": "USR web port",
          "usr_username": "USR username",
          "usr_password": "USR password",
          "verify_usr_web": "Verify the USR web login",
          "watchdog_silence": "Seconds without a valid state before querying",
          "watchdog_timeout": "Seconds to wait for each watchdog query"
        },
        "data_description": {
          "usr_host": "Use the purifier/USR module's LAN IP address. A DHCP reservation or static address is recommended.",
          "listen_port": "Configure the purifier's Socket B TCP client to connect to the Home Assistant LAN IP on this port. Each purifier needs a different listening port.",
          "usr_username": "Default USR modules commonly use admin.",
          "usr_password": "Default USR modules commonly use admin.",
          "verify_usr_web": "Checks reachability and HTTP Basic/Digest credentials. The integration does not change USR settings automatically.",
          "watchdog_silence": "The purifier normally reports about every 15 seconds. The default 45 seconds avoids unnecessary polling."
        }
      },
      "reconfigure": {
        "title": "Reconfigure AirProce",
        "description": "Update purifier, Socket B port, USR login, or watchdog settings. The integration reloads automatically after saving.",
        "sections": {
          "basic": {"name": "Purifier"},
          "advanced": {"name": "Advanced settings"}
        },
        "data": {
          "device_name": "Device name",
          "device_model": "Purifier model",
          "usr_host": "USR module IP address",
          "listen_port": "Socket B listening port",
          "usr_web_port": "USR web port",
          "usr_username": "USR username",
          "usr_password": "USR password",
          "verify_usr_web": "Verify the USR web login",
          "watchdog_silence": "Seconds without a valid state before querying",
          "watchdog_timeout": "Seconds to wait for each watchdog query"
        },
        "data_description": {
          "listen_port": "Each purifier must use a unique Socket B listening port."
        }
      }
    },
    "error": {
      "cannot_connect_usr": "Cannot connect to the USR module web interface.",
      "invalid_usr_auth": "The USR username or password was rejected.",
      "listen_port_in_use": "Another AirProce entry already uses this Socket B listening port.",
      "invalid_config": "The configuration is invalid."
    },
    "abort": {
      "already_configured": "This AirProce purifier is already configured.",
      "wrong_device": "The internal stable device ID does not match the existing entry."
    }
  },
  "entity": {
    "sensor": {
      "temperature": {"name": "Temperature"},
      "humidity": {"name": "Humidity"},
      "pm25": {"name": "PM2.5"},
      "voc": {"name": "VOC"}
    }
  }
}
